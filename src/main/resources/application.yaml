#spring.application.name=ConfigServer
server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/venkateshpalagiri/microservice-config
          clone-on-start: true
